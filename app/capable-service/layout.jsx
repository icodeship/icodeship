'use client';

import Capable_service_layout from '@/Pages/Capable_service_layout';

export default function CapableServiceLayout({ children }) {
  return <Capable_service_layout>{children}</Capable_service_layout>;
}
